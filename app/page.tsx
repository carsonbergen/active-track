import Workspace from "@/app/components/Workspace";

export default function Home() {
    return (
        <div className={`w-scren h-screen p-8 bg-gradient-to-br from-black via-green-200 to-black font-outfit`}>
            <Workspace/>
        </div>
    );
}
